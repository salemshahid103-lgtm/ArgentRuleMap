# Fixture M Instructions

Here is a valid reference to [index file](src/index.ts) and inline `src/index.ts`.
Also external link [Google](https://google.com) and mailto [Email](mailto:dev@example.com).
Also standard code `const x = 1`, `v1.2.3`, `--strict`, and `react`.

Here is a stale markdown link: [Missing Doc](docs/missing-guide.md).
Here is a stale inline path: `src/nonexistent/service.ts`.
