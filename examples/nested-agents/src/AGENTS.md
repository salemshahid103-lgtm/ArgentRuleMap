# Source Directory Instructions

## Scope
Applies specifically to all files within `src/`.

## Architecture Rules
- All API handlers in `src/api` must validate incoming requests.
- Return structured error payloads with appropriate HTTP status codes.
